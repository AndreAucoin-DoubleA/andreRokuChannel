namespace player

    interface Params
        movieId as string
        optional title as string
        optional description as string
        optional backdropUri as string
        optional streamUrl as string
        optional streamFormat as string
        optional trackProgress as boolean
    end interface

end namespace
