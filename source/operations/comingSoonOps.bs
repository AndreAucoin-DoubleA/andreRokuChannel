import "pkg:/source/navigation/routes.bs"

namespace comingSoonOps

    function titleFor(subtype as string) as string
        for each route in routes.all()
            if route.component = subtype then return route.label
        end for

        return ""
    end function

end namespace
